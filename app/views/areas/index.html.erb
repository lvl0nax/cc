<h1>Список сфер</h1>

<table>
  <tr>
    <th>Название</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @areas.each do |area| %>
  <tr>
    <td><%= area.name %></td>
    <td><%= link_to 'Показать', area %></td>
    <td><%= link_to 'Редактировать', edit_area_path(area) %></td>
    <td><%= link_to 'Удалить', area, confirm: 'Вы уверены?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить новую сферу', new_area_path %>
