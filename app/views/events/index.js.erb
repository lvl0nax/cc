$('.monih12').append("<%= j render(@events) %>");
<% if @events.next_page %>
	$(".pagination").replaceWith("<%= j will_paginate(@events) %>");
<% else %>
	$(".pagination").hide();
<% end %>
<% sleep 0.5 %>
