<% content_for :head do %>
<div class="title"><h2>МОЙ КАЛЕНДАРЬ (<%= @user.active_events %>)</h2></div>
<% end if current_user == @user %>

<ul id="myitems" class="items">
	<h1> 		
		КАЛЕНДАРЬ <%= @user.name %> | <%= link_to "СОЗДАННЫЕ МЕРОПРИЯТИЯ", userevents_user_path(@user) %></h1>
	<%= render @actions %> 
</ul>